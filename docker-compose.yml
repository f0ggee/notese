version: '3.8'



services:
  app:
    image: fffffoggy/app13:latest
    ports:
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro

    depends_on:
      - db


  db:

    image: postgres:latest
    restart: always
    environment:
      POSTGRES_DB: default_db
      POSTGRES_USER: gen_user
      POSTGRES_PASSWORD: Oh~IAjEOQ=@0dc
    volumes:
      - pg_data:/var/lib/postgresql/data


volumes:
  pg_data:

